# 快速视觉复核

运行 `npm run preview`。脚本会先构建、自动选择 3000–3100 的空闲端口，并仅在 HTTP 服务可达后输出预览链接。

每次视觉改动只检查受影响模块；默认使用两个关键视口：

- 桌面：1440 × 900
- 移动：390 × 844

涉及“关于我”一屏布局时，额外检查 1200 × 615，确认底部没有被裁切。涉及项目弹窗时，仅逐一打开三个项目卡片检查标签和内容面板的连接状态。
